#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
   n = len(my_list)
   if idx < n:
       return my_list
   elif idx >= n:
       return my_list
   else:
       my_list.replace(my_list)
       return my_list
